@model IEnumerable<SportsStore.Models.Domain.Product>

@{
    ViewBag.Title = "Products";
    ViewBag.Active = "Product";
}

<h2>@ViewBag.Title</h2>


    @*@using (Html.BeginForm("Index", "Product"))
    {*@
        <div class="form-inline">
            <div class="form-group">
                @Html.Label("CategoryId", "Category")
                @Html.DropDownList("CategoryId", ViewBag.Categories as SelectList, "--all categories--", new { @class = "form-control" })
            </div>
            @*<button type="submit" class="btn btn-default">Submit</button>*@
            @Html.ActionLink("New Product", "Create",null, new {@class= "btn btn-default text-right" })
            </div>
      @*}*@
<p></p>

<div id="products">
    @Html.Partial("Products", Model)
</div>

@section Scripts{
    <script src="~/Scripts/CustomScripts/Products.js"></script>
}